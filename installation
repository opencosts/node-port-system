1. npm init -y
2. npm install express mongoose body-parser cors
3. npm install --save-dev typescript @types/node @types/express @types/cors ts-node nodemon
4. npx tsc --init


Created a new tsconfig.json with:
                                                                                                                     TS
  target: es2016
  module: commonjs
  strict: true
  esModuleInterop: true
  skipLibCheck: true
  forceConsistentCasingInFileNames: true


You can learn more at https://aka.ms/tsconfig




🔐 Generate RSA Key Pair for JWT (RS256) in git bash
✅ Step 1: Generate a Private Key
openssl genpkey -algorithm RSA -out private.key -pkeyopt rsa_keygen_bits:2048

✅ Step 2: Generate the Public Key from the Private Key
openssl rsa -in private.key -pubout -out public.key